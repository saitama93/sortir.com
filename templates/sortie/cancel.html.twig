{% extends 'base.html.twig' %}

{% block title %}Annuler une sortie{% endblock %}

{% block body %}
    <h1 class="text-center mt-5 mb-3">Annuler {{sortie.nom}}</h1>

    
        <div class="row">
            <div class="col-xs-12">


                {{ form_start(form) }}
                
                 {{ form_row(form.motifAnnulation) }}
                <div class="d-flex justify-content-center items-center">
                    <button type="submit" class="btn btn-info mb-3">Annuler</button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    
{% endblock %}
